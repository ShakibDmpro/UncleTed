<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- This allows the FileProvider to share files from the app's internal "files" directory,
         where CameraHandler saves photos and videos. -->
    <files-path name="shared_files" path="." />
</paths>